# セルフアチーブ WordPress 本番公開チェックリスト

本番ドメイン: `https://selfachieve.jp/`
現在のステージング: `https://htmlacheive.com/wp/`

---

## 1. WordPress 基本設定

- [ ] **WordPress アドレス（URL）** を `https://selfachieve.jp` に変更  
  `設定 → 一般 → WordPress アドレス（URL）`
- [ ] **サイトアドレス（URL）** を `https://selfachieve.jp` に変更  
  `設定 → 一般 → サイトアドレス（URL）`
- [ ] パーマリンク設定を一度保存し直す（`.htaccess` を再生成）  
  `設定 → パーマリンク → 変更を保存`

---

## 2. SEO / canonical URL【重要】

- [ ] **canonical URL が `/wp/` なしの本番ドメインになっていることを確認**  
  任意のページのソースを表示し、`<link rel="canonical" href="https://selfachieve.jp/...">` となっていることを確認する  
  ※ ステージング環境では `https://htmlacheive.com/wp/...` になっているため、URLを変更後に必ず再確認すること
- [ ] AIOSEO → 一般設定 → サイト情報 のURLが本番ドメインになっていることを確認
- [ ] Google Search Console に本番ドメインのプロパティを追加・認証
- [ ] サイトマップ（`/sitemap.xml`）を Search Console に送信

---

## 3. OGP / メタタグ

- [ ] トップページの `og:url` が本番ドメインになっていることを確認
- [ ] `og:image` のURLが本番ドメインになっていることを確認（`/wp/` が含まれていないこと）
- [ ] Facebook / X（旧Twitter）のカードデバッガーで表示を確認  
  - Facebook: https://developers.facebook.com/tools/debug/  
  - X: https://cards-dev.twitter.com/validator

---

## 4. 構造化データ（JSON-LD）

- [ ] トップページ・各サービスページのソースで `@id` の URL が本番ドメインになっていることを確認  
  ※ `inc/schema.php` 内の URL は `home_url()` を使用しているため、WordPress URL設定変更後に自動反映される

---

## 5. リダイレクト設定

- [ ] 旧静的サイトの URL（例: `selfachieve.jp/seo/`）が WordPress の URL に正しく到達することを確認
- [ ] `selfachieve.jp/wp/` へのアクセスは不要なため、`/wp/` プレフィックスなしで動作することを確認

---

## 6. Basic認証

- [ ] 本番公開前に Basic認証を**解除**する
- [ ] 解除後、Google リッチリザルトテストで主要ページを検証  
  https://search.google.com/test/rich-results

---

## 7. アナリティクス / タグマネージャー

- [ ] GA4 の計測タグが正しく動作していることを確認
- [ ] GTM（使用している場合）のコンテナが本番ドメインに対応していることを確認

---

## 8. 最終表示確認

- [ ] トップページ・各サービスページ・コラム詳細・実績詳細をPC・スマホで目視確認
- [ ] お問い合わせフォームの送信テスト（テスト送信後に削除）
- [ ] 404ページの表示確認

---

*最終更新: 2026-05-23*
